steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "build",
        "-t",
        "us-west3-docker.pkg.dev/$PROJECT_ID/wtwr-backend/wtwr-backend:$COMMIT_SHA",
        ".",
      ]
images:
  - "us-west3-docker.pkg.dev/$PROJECT_ID/wtwr-backend/wtwr-backend:$COMMIT_SHA"
options:
  logging: CLOUD_LOGGING_ONLY
